// lib/screens/anger_page.dart
import 'package:flutter/material.dart';

class AngerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Anger Page")),
      body: Center(child: Text("This is the Anger Page")),
    );
  }
}